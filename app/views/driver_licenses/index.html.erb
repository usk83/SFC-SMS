<div class="col-lg-12">
  <%- model_class = DriverLicense -%>
  <div class="page-header">
    <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
  </div>
  <table class="table table-striped">
    <thead>
      <tr>
        <th><%= model_class.human_attribute_name(:license_type) %></th>
        <th><%=t '.actions', :default => t("helpers.actions") %></th>
      </tr>
    </thead>
    <tbody>
      <% @driver_licenses.each do |driver_license| %>
        <tr>
          <td><%= driver_license.license_type %></td>
          <td>
            <%= link_to t('.edit', :default => t("helpers.links.edit")),
                        edit_driver_license_path(driver_license), :class => 'btn btn-default btn-xs' %>
            <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                        driver_license_path(driver_license),
                        :method => :delete,
                        :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                        :class => 'btn btn-xs btn-danger' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <%= link_to t('.new', :default => t("helpers.links.new")),
              new_driver_license_path,
              :class => 'btn btn-primary' %>
</div>
