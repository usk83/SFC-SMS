<%= form_for(@user_shift) do |f| %>
  <% if @user_shift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_shift.errors.count, "error") %> prohibited this user_shift from being saved:</h2>

      <ul>
      <% @user_shift.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :task_shift_id %><br>
    <%= f.number_field :task_shift_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
