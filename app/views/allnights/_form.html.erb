<%= form_for(@allnight) do |f| %>
  <% if @allnight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@allnight.errors.count, "error") %> prohibited this allnight from being saved:</h2>

      <ul>
      <% @allnight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
