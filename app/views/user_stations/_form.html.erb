<%= form_for(@user_station) do |f| %>
  <% if @user_station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_station.errors.count, "error") %> prohibited this user_station from being saved:</h2>

      <ul>
      <% @user_station.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :station_id %><br>
    <%= f.text_field :station_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
